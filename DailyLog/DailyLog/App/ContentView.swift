import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("DailyLog")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
